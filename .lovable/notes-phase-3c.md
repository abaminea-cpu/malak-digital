# Phase 3C — état

Livré sans configuration :
- Notifications admin temps réel (toast + son) via Supabase Realtime sur `orders`
- Helpers WhatsApp `wa.me` (confirm / shipped / abandoned)

À configurer plus tard (différé par l'utilisateur) :
1. Domaine email (`notify.malakdigital.com`) → Lovable Emails
2. SMS Algérie — provider à choisir (NetBeOpen / IdoomSMS / Twilio)
3. WhatsApp Business API officielle (optionnel)
